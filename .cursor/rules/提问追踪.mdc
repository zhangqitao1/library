---
description: 用户提问的自动追踪日志（学情/提问记录.jsonl）的说明与用法。当家长/小雨想了解"她最近都在问什么、关心什么、哪类问题反复出现"，或要做提问周报/学习兴趣分析时使用。
alwaysApply: false
---

# 提问追踪机制

## 自动记录（无需人工）
- 钩子 `.cursor/hooks/log-prompt.py`（配置见 `.cursor/hooks.json` 的 `beforeSubmitPrompt`）会在**每次用户发送消息前**自动把提问追加到 `学情/提问记录.jsonl`。
- 每行一条 JSON：`time`（北京时间）、`prompt`（原话）、可能有 `conversation_id`、`_src`（命中的字段名）。识别不到文本时存 `_raw_keys`/`_raw` 便于排查。
- 这是观察型钩子，失败也不会阻断对话；异常记到 `学情/.hook-error.log`。

## 怎么用这份数据（让记录产生价值）
当被要求"追踪/分析小雨想了解什么"时：
1. 读 `学情/提问记录.jsonl`，按时间/科目/主题归类。
2. 找**高频与反复出现**的问题 → 往往就是她的兴趣点或真实薄弱区，反哺 `学情/小雨-学情档案.md` 的薄弱点与兴趣画像。
3. 需要时输出一份简短"提问周报"（她最近关心啥、问得最多的科目、值得主动补强的点）。

## 维护
- 文件持续增长属正常；如需归档，按月另存为 `学情/提问记录-YYYY-MM.jsonl`，不要删原始数据。
- 真实触发一次后，若发现 `prompt` 为 null、只有 `_raw_keys`，按其中真实字段名更新 `log-prompt.py` 的 `PROMPT_KEYS`。
